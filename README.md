# Smart Resume Analyzer
NLP-based resume classifier using TF-IDF + Soft Voting Ensemble (LR + SVC + MLP)

## Run the app
pip install -r requirements.txt
streamlit run app.py

## Dataset
Updated Resume Dataset — 2,484 samples, 25 categories
